# ID 9：共焦点绑定执行

日期：2026-09-24。范围：第二条固定执行链；旧求解／训练流程尚未迁移。

## 本批结果

只输入原题 `fact_expressions` 和 `query_expressions`，执行 RM3→RM11→RM5→RM12，得到 **t=9**。模型顺序由诊断入口指定，不读取数据集 `models`、`process` 或答案作决策。

| 转换 | 新增信息 |
| --- | --- |
| RM3 / H | H.a²=25、H.b²=9、原点中心及 x 轴方向 |
| RM11 / H | H.c²=16 |
| RM5 / G | G.a²=7、G.b²=t、原点中心及 x 轴方向；从双曲线类型推导并记录 t>0 |
| RM12 / G、H、共焦点关系 | 公共操作实例化 G.c²=H.c²=16，再由 16=7+t 求 t=9；统一回代 G.b²=9 |

[实际 trace](artifacts/id9_bound_trace.json) 使用 `bound-slice-v2`，含对象／关系绑定、框架、推导条件、来源、候选根及归约。直接读取终态不再求解。换序 RM5→RM3→RM11→RM12 也通过，不把原始标注序列当成唯一顺序。

## 代码变化

- `transition_state.py`：给定焦点集合相等关系 `FocusEquality`；由模型生成的 `CurveFrame`。初始化不执行关系推导。
- `transition_primitives.py`：共用标准分母提取、椭圆参数、共焦点实例化及有限条件矛盾检查。
- `bound_application.py`：动作增加 `relation_id / peer_curve / peer_equation_id`；统一提交属性、赋值、推导条件与几何框架，校验曲线／方程归属。
- 原 `model_003/011/012.py` 增加绑定模式；`model_005.py` 补类型推导条件及框架。旧接口保留。
- `bound_slice.py`：共享动作回放、共焦点入口和 CLI 模式；新测试在 `tests/test_bound_shared_focus.py`，并修订旧 RM5 的未知符号边界测试。

## 验证与边界

运行及回归命令：

```bash
python3 -m src.reasoning.bound_slice --mode shared-focus --problem-id 9
python3 -m pytest -q tests/test_bound_shared_focus.py tests/test_bound_transition_slice.py tests/test_theorem_missing_models.py tests/test_solver_symbolic.py tests/test_answer_extractor_symbolic_solver.py tests/test_evaluation_protocol.py
```

结果：**113 passed**，其中共焦点测试 34 项。包含第三曲线隔离、错误关系／方程绑定、缺失前提、条件矛盾、冲突回滚、多根保留、重复执行、合法换序与旧接口兼容；改换椭圆参数可得到不同答案或拒绝不构成双曲线的解。

- 仅支持原点中心、x 轴焦向的标准椭圆—双曲线；平移、y 轴、旋转及其他曲线组合未扩展。公共关系操作只实例化给定关系，不暗中计算椭圆 c²。
- RM11 新接口仅正向求 c²；RM12 仅共焦点参数约束模式。非共焦点的一般参数关系仍待补充。
- RM5 只有在 x² 分母为正可确认时，才由双曲线类型推导 b²>0；轴向未确定时不假定方向。此规则替代旧测试中的“一切未知分母符号均不适用”。
- 标量求解仍限一元至多二次；多个绑定、多解或未判定条件不静默选择。条件一致性检查不是完整多元约束求解器。
- 每条曲线当前只接受一个标准方程框架绑定；等价多方程合并、全局多关系一致性与分支搜索尚未实现。trace 为诊断证据，不是最终训练数据。

下一批建议补 RM21 正向和 RM11/RM12 普通参数关系模式；继续积累小批可执行轨迹，再统一旧轨迹构建与自主求解入口。
