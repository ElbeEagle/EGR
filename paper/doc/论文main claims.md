#  圆锥曲线问题推理求解



### 1. 研究对象（Research Object）
* **研究对象**：**圆锥曲线问题推理求解（Conic-Section Problem Solving）**。

* **学术定位**：属于复杂解析几何领域的知识驱动推理与神经符号计算，是此前函数题求解（TDF）工作的纵深拓展。

  

---

### 2. 圆锥曲线的核心特征与求解困难
> 解析几何既不同于“纯代数方程”，也不同于“几何证明”，具备三大本质特征：

1. **“几何实体属性” 与 “代数参数变换” 深度耦合**：解题高度依赖“标准方程建立 $\leftrightarrow$ 内在参数提取（$a, b, c, p$） $\leftrightarrow$ 几何关系转化”的交替推演；
2. **推理步骤长**：相较于 代数题、算术题，推理步骤明显更长。
3. **应用定理时严格依赖当前的题目**：定理不能任意调用，必须在当前状态已具备相应几何实体与参数时才可触发。


**“几何实体属性” 与 “代数参数变换” 深度耦合**：解题高度依赖“标准方程建立 $\leftrightarrow$ 内在参数提取（$a, b, c, p$） $\leftrightarrow$ 几何关系转化”的交替推演，具体内含：

单步推理中，包含 ”几何 <—> 代数“ 的双向对应（包含4种投射）：

1. **身份投射：** 几何角色 $\rightarrow$ 符号的代数身份（焦点在哪轴 \(\Rightarrow\) 谁是 \(a^{2}\)）。
2. **关系投射：** 几何对象 $\rightarrow$ 代数式（渐近线 \(\Rightarrow\) \(y=\pm(b/a)x\)，不是 \(e=c/a\)）。
3. **回代投射：** 代数运算/变换 $\rightarrow$  结果写回几何对象（\(2p=4\Rightarrow F(0,1)\)、准线）。
4. **缺口投射：** 查询所缺的几何量，决定下一步补哪个参数（要 \(e\) 就缺 \(c,a\)；要 \(|PF|\) 就缺焦点或准线）。



>  几何与代数如何耦合（示例）

### 例 1:

\[ \frac{x^2}{4}-\frac{y^2}{m^2}=1,\qquad 5x-2y=0, \]

求 \(m\)，模型序列为 `[5, 21]`。

完整认知过程是：

1. 几何定位：给出的直线不是普通直线，而是该双曲线的一条渐近线。
2. 代数兑现：标准方程给出 \(a=2,b=m\)，渐近线模式给出斜率 \(b/a=m/2\)。
3. 关系对齐：已知直线写成 \(y=\frac52x\)，因此 \(m/2=5/2\)。
4. 参数回写：得到 \(m=5\)，从而闭合目标缺口。

这里不能先进行任意消元；“渐近线”这个几何身份决定应该比较斜率 \(b/a\)。

### 例 2：代数求出 \(a\) 之后，必须写回焦点/准线，定义才能用

题 5：\(x^{2}=ay\) 过 \(A(1,\tfrac{1}{4})\)，求 \(A\) 到焦点的距离。过程 `[9, 2, 29, 17]`。

1. **几何 \(\rightarrow\) 代数：** \(A\) 在抛物线上 \(\Rightarrow\) 代入 \(1=a\cdot\tfrac{1}{4}\Rightarrow a=4\)。
2. **代数 \(\rightarrow\) 几何：** \(x^{2}=4y\) 被读成开口向上的标准抛物线 \(\Rightarrow\) 焦点 \(F(0,1)\)、准线 \(y=-1\)（RM 9, 29）。
3. **几何 \(\rightarrow\) 代数：** 定义 \(|PF|=\) 到准线距离 \(\Rightarrow\) \(\tfrac{1}{4}-(-1)=\tfrac{5}{4}\)，而不必对未知焦点硬上两点距离。

若停在「算出 \(a=4\)」却不把方程重新解释为焦点/准线，查询「到焦点距离」在几何上仍是空角色。这就是「代数打开下一步几何」。

---

### 3. 核心思想：基于状态转换的求解范式（Core Paradigm）
* **核心思想**：将圆锥曲线解题建模为一个**基于题目状态和定理模型的状态转换求解范式（State-Transform Solving Paradigm）**，本质是状态驱动的序贯决策过程（Next Reasoning Model Prediction）。
* **基本求解链条**：
  $$S_0 \xrightarrow{M_0} S_1 \xrightarrow{M_1} \cdots \xrightarrow{M_{T-1}} S_T \xrightarrow{\text{Extract}} \hat{a}$$
  * 输入为 $x = (F, q)$（初始已知事实与求解目标）；
  * 每一步根据当前推理状态 $S_t$，策略选择最适用的推理模型 $M_t$；
  * 执行确定性状态转移得到 $S_{t+1}$，直至终止状态 $S_T$ 抽取最终答案 $\hat{a}$。

---

### 4. 题目状态：双层状态建模（Dual-Layer Representation）
> 解决“神经网络直觉感知”与“符号系统精确演算”在表示上的天然矛盾：

* **符号表示（$S^{\text{sym}}$）**：精确记录几何实体、代数方程、已知参数与约束关系，面向符号求解器进行**精确符号计算**；
* **抽象表示（$S^{\text{abs}}$）**：将符号表示映射为结构化特征向量（曲线类型、已知/未知参数分布、目标依赖等），面向神经网络进行**模型预测与选择**。

预先整理**有限个抽象表示**（目前18类）；每一步推理时将“**符号表示**”映射成“**抽象表示**”candidate；“抽象表示”与 “reasoning pattern” 有对应；执行时再回到符号表示。

| 抽象描述的示例（初步18类）                     | 推理 pattern               |
| ---------------------------------------------- | -------------------------- |
| 椭圆 + x 轴 + 有方程 + 缺 e                    | 3 → 11 → **13**            |
| 双曲线 + x 轴 + 有方程 + 缺 e                  | 5 → 12 → **13**            |
| 双曲线 + 有方程 + 有渐近线 + 缺 m 或渐近线方程 | 5 → **21**                 |
| 双曲线 + 有渐近线 + 缺 e                       | 5 → 21 → **13**            |
| 双曲线 + 已给 e + 缺参数/方程                  | **13** 反解 → 11/12/21     |
| 抛物线 y2=⋅ + 有方程 + 缺准线/焦点             | 7 → **29**                 |
| 抛物线 + 点在曲线上 + 缺 ∣PF∣                  | 7/9 → **2** → 29           |
| 椭圆/双曲线 + 点 + 两焦点 + 缺周长/角          | **0/1**                    |
| 有弦/相交 + 缺参数或斜率                       | **42/43**，78              |
| 有中点                                         | **54**，44–46              |
| 缺面积 + 焦点三角形                            | **56**，30–32              |
| 缺范围/最值 + 有交                             | **65**，63，79             |
| 有切线 / 圆                                    | **38/39**，**75/76**       |
| 两曲线 + 焦点重合                              | 各自 3/5 → 11/12 通 c      |
| 仅有方程 + 缺轴长                              | 3/5 → **11/12**            |
| 定向为 y 轴                                    | **4 或 6**（不要误用 3/5） |
| 有点到直线距 + 渐近线                          | **52**，22                 |
| 有垂直                                         | **61**，49                 |



---

### 5. 定理模型：80 个 RM 的“原材料与构建逻辑”（Reasoning-Model Pool）
> 解释“转换”从何而来、为何有效：

* **原材料**：
  * 提取自高中解析几何课程标准、知识本体与经典解题方法（覆盖椭圆、双曲线、抛物线、圆的几何定义、标准方程性质、焦点/准线/渐近线关系及典型代数变换规则）。
* **构建逻辑**：
  * 由一组通用的符号计算原语（Symbolic Primitives）（如方程化简、联立消元、判别式计算、根与系数关系等）组合而成；
  * 通过 Conic10K 语料库的实证检验，覆盖了绝大多数典型题目（90%+）所需的推导动作。；
  



定理模型的来源是 **识别上一步到下一步的、推理缺口**：

1. 看查询 \(q\)：终态必须出现的几何量/代数对象是什么。
2. 看当前条件：哪些角色已填，哪些还空。
3. 若空的是某实体/参数，而当前已有它的几何宿主，则存在一条合法转换：用该宿主上的几何–代数耦合，把缺口补上。
4. 这条转换就是一个 pattern / RM：\(\mathrm{pre}=\)「宿主已在、缺口仍空」；\(\mathrm{trans}=\)「执行那次投射并写回」。





## 两种题型的典型区别

**GAP 是欧氏平面几何的「图形构型 → 代数方程」问题；**

**圆锥曲线题是解析几何的「曲线身份 ↔ 方程参数」问题。**

 geometry–algebra problem，是：从图形中识别并扩展一个欧氏几何构型，再用代数求解构型上的数量（用全等、相似、勾股、面积这些定理，对边长/角度/面积列出可算的式子）。

conic-section problem，是：同一个二次曲线对象同时带着几何角色和代数载体，每一步都在「定位 概念的几何身份」和「提取参数/代数变换」之间投射。



geometry–algebra problem难在**图上没写明的构型**。主要解决“当前隐藏着什么几何构型，它允许建立什么方程”；

conic-section problem难在**几何身份和代数表达式的依赖**。主要定位对象/概念的几何身份，它允许提取的具体参数代数式/方程”；