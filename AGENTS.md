# EGR 开发约定

## 开发前回顾

1. 先读 `doc/project_structure.md`，了解当前实现、接入状态和后续方向。
2. 查阅 `dev_record.md` 最近及与任务相关的记录；修改接口时读 `doc/api_reference.md`。
3. 涉及状态、模型或公共操作时，从 `docs/开发规格/README.md` 定位对应接口、案例、模型索引和实现记录；核对 `model/` 中的原始定义。
4. 核对实际代码、测试和 `git status`。简要说明可复用模块与本次修改范围，再动手；不要根据历史文档重复实现已有功能。

当前实现地图描述现状，开发规格描述设计，开发记录保存历史。三者与代码不一致时，先核实并修订受影响条目；历史统计不自动视为当前结果。用户最新确认的范围和决策优先。

## 项目边界

- 主线为双层状态、80 个高层推理模型、模型选择与应用、共享符号操作和可信轨迹。既有原型质量不能限制研究目标，也不能被当作目标已实现的证据。
- 当前先完善绑定执行与轨迹，再训练选择器；Entropy／求解进度机制和 LLM 实验后置，除非用户明确调整。
- 新绑定执行链与旧求解／训练流程并存。区分“已有代码、独立验证、已接入、计划功能”；不得把 RM5→RM21 固定链描述成完整自主求解系统。
- 数据集 `models` 是 Opus 辅助生成的弱标注，不保证真实执行顺序。保留原标注，另行记录经执行验证的轨迹；按记录 `id` 定位题目。
- 正式求解输入不得依赖参考答案、`process` 或 gold 模型序列。指定动作回放属于单独诊断，不冒充自主预测。
- 模型明确对象绑定、前提和输出；共享计算复用公共操作。新状态变化统一经应用器提交，记录来源；多解、条件不足、重复执行与冲突不得静默处理。答案整理不隐藏新的定理推导。

## 修改与验证

- 优先小而可审阅的改动，不做未请求的大范围重构。编辑前用 3–6 条列出文件和计划。
- 先搜索再引用 API、配置或路径，不凭空假设。保持现有风格、类型约束和明确错误处理；仅添加必要注释。
- 不覆盖或撤销已有未提交修改，不泄露凭据；需要密钥时使用环境变量。不添加未请求的网络调用、分析或遥测。
- 执行命令前说明准确命令及用途。行为变化补充对应测试，先跑最快相关检查；只有新增风险或失败才扩大验证。
- 文档改动检查链接、接口对应关系和示例；不要为纯文档重写进行大规模训练或评测。

## 开发后同步记录

- `doc/project_structure.md`：仅更新受影响的关键文件职责、能力／接入状态及下一步。
- `doc/api_reference.md`：仅更新变化的接口、行为与适用范围，避免复制源码和进度统计。
- `dev_record.md`：保留旧记录，追加日期、变更、文件、验证及剩余限制(简要说明)；长说明链接到对应规格／实现记录。
- `docs/开发规格/`：仅在职责、设计或案例变化时更新。文档应简短，避免多处重复同一份功能清单。
- 回复默认中文，给出简短摘要、改动文件和验证结果。代码任务附 <=72 字符的 commit summary，以及包含 Current date、What done/changed、Effect、Which files added/updated、Next step work 的 commit description；生成说明不等于自动提交。


